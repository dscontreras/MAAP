classdef MultipleObjectDisplacementOperation < DisplacementOperation

    methods
        function obj = MultipleObjectDisplacementOperation(src, axes, table, error, img_cover, pause_button, pixel_precision,
            max_displacement, resolution, draw, error_report_handle)

        end
    end

end
